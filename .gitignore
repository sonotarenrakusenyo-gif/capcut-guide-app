# 依存パッケージ（npm install で再生成できるため除外）
node_modules/

# ビルド成果物（npm run build で再生成できるため除外）
dist/

# 環境変数（秘密情報を含む可能性があるため除外）
.env
.env.local
.env.*.local

# OS 生成ファイル
.DS_Store
Thumbs.db

# エディタ設定
.vscode/settings.json
*.swp
*.swo

# ログ
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vercel
.vercel
