/**
 * Geminiフィードバック反映パッチ（YouTubeシリーズ前半 4か所）
 * youtube-002（2か所）/ youtube-003 / youtube-005
 */
import fs from "fs";
import path from "path";
import { fileURLToPath } from "url";

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const root = path.join(__dirname, "..");

// ---- パッチ定義 ----

const patches = [
  // 1. youtube-002：「デザインの注意点」にタイムスタンプ右下注意を追記
  {
    id: "youtube-002",
    label: "サムネイル：右下タイムスタンプ注意",
    from: `## デザインの注意点
- YouTubeのプレイヤー上では**タイトルテキスト**が下に表示されるため、サムネイルとタイトルで同じ情報を繰り返さず、**互いを補い合う**内容にすると良いです
- スマホでの表示サイズはかなり小さいため、**細かい文字・細い線**は見えにくくなります`,
    to: `## デザインの注意点
- YouTubeのプレイヤー上では**タイトルテキスト**が下に表示されるため、サムネイルとタイトルで同じ情報を繰り返さず、**互いを補い合う**内容にすると良いです
- スマホでの表示サイズはかなり小さいため、**細かい文字・細い線**は見えにくくなります
- **右下には文字や重要な顔を置かない**：YouTubeは動画の右下に**再生時間（タイムスタンプ）**を自動表示します。この位置に大事な要素を置くと隠れてしまうため、必ず空けておきましょう`,
  },

  // 2. youtube-002：「上達のコツ」にA/Bテスト機能を追記
  {
    id: "youtube-002",
    label: "サムネイル：A/Bテスト機能を追記",
    from: `## 上達のコツ
自分のチャンネルのサムネイルを**統一したデザインルール（フォント・色・文字の配置）**にすると、ブランドとして認識されやすくなります。最初は3要素（色・フォント・テキスト位置）だけ揃えるところから始めましょう。`,
    to: `## 上達のコツ
自分のチャンネルのサムネイルを**統一したデザインルール（フォント・色・文字の配置）**にすると、ブランドとして認識されやすくなります。最初は3要素（色・フォント・テキスト位置）だけ揃えるところから始めましょう。

2026年現在は、**YouTube Studioのサムネイルテスト機能（Test & Compare）**を使って2〜3種類のサムネイルを比較し、一番クリックされたものを自動で採用する方法が主流になっています。デザインに迷ったときはデータに判断させるのが最も確実です。`,
  },

  // 3. youtube-003：「アウトロを作る」ステップにスマホのカード配置注意を追記
  {
    id: "youtube-003",
    label: "イントロ・アウトロ：スマホのカード配置注意",
    from: `### ステップ4：アウトロを作る（エンドカード）
1. 動画の末尾に**10〜15秒の空間**を作ります（黒画面または静止画を置く）
2. **「テキスト」**で「チャンネル登録はこちら！」「次はこの動画→」などの案内テキストを追加
3. YouTube Studioの**エンドカード機能**と組み合わせると、クリック可能なボタンが重なって表示されます`,
    to: `### ステップ4：アウトロを作る（エンドカード）
1. 動画の末尾に**10〜15秒の空間**を作ります（黒画面または静止画を置く）
2. **「テキスト」**で「チャンネル登録はこちら！」「次はこの動画→」などの案内テキストを追加
3. YouTube Studioの**エンドカード機能**と組み合わせると、クリック可能なボタンが重なって表示されます
4. **スマホ視聴への注意**：スマホでは画面の端にUIが重なりやすく、エンドカードの表示範囲が狭くなります。誘導ボタンや文字は**画面の端に寄りすぎず、中央寄りにスペースを空けて配置**しておくと、スマホでも見やすくなります`,
  },

  // 4. youtube-005：「ステップ4：スタイルを統一する」に一括編集の補足を追記
  {
    id: "youtube-005",
    label: "字幕：一括編集（Batch Edit）の時短術を追記",
    from: `### ステップ4：スタイルを統一する
- 字幕クリップを1つ選択→**「スタイル」**でフォント・色・縁取りを設定→**「すべてに適用」**で全字幕に一括反映します`,
    to: `### ステップ4：スタイルを統一する
- 字幕クリップを1つ選択→**「スタイル」**でフォント・色・縁取りを設定→**「すべてに適用」**で全字幕に一括反映します
- **「一括編集（Batch Edit）」**ボタンを活用すると、60分以上の長い動画でも数秒で全テロップのフォント・サイズ・位置をまとめて変えられます。長尺コンテンツを作る方は必ずマスターしておきたい時短機能です`,
  },
];

// ---- 3つのJSONファイルに適用 ----
const targets = [
  "articles-data.json",
  "shared/articles-data.json",
  "src/data/articles-data.json",
];

for (const rel of targets) {
  const fp = path.join(root, rel);
  const raw = fs.readFileSync(fp, "utf8");
  let data = JSON.parse(raw);

  data.articles = data.articles.map((a) => {
    // 同一IDに複数パッチがある場合を順番に適用
    let content = a.content;
    let patched = false;
    for (const p of patches) {
      if (p.id !== a.id) continue;
      if (!content.includes(p.from)) {
        console.warn(`⚠️  テキストが見つかりません [${p.label}] in ${rel}`);
        continue;
      }
      content = content.replace(p.from, p.to);
      patched = true;
    }
    return patched ? { ...a, content } : a;
  });

  fs.writeFileSync(fp, JSON.stringify(data, null, 2));
}

const ids = [...new Set(patches.map((p) => p.id))];
console.log("✅ パッチ適用完了:", ids.join(", "), `（${patches.length}か所）`);
